version: 1
applications:
  - appRoot: frontend
    platform: web
    framework: react
    buildPath: /
    baseDirectory: build
    name: fire-index
    phases:
      build:
        commands:
          - npm run build
      postBuild:
        commands:
          - echo "React app build complete"
